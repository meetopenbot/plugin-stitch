{
  "name": "@meetopenbot/stitch",
  "version": "0.0.1",
  "type": "module",
  "description": "Stitch agent plugin for OpenBot",
  "main": "./dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --format=esm --outfile=dist/index.js --packages=external",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.10.1",
    "esbuild": "^0.21.0"
  },
  "dependencies": {
    "@google/stitch-sdk": "^0.3.5",
    "@meetopenbot/plugin-sdk": "^0.1.1"
  }
}
